import React, { useState } from 'react';
import { AppBar, Toolbar, Typography, Tabs, Tab, Button, Drawer, List, ListItem, ListItemText, makeStyles } from '@material-ui/core';
  
  const Customers = () => {   
      return (
        <div >
 <Typography variant="h6">Customers page</Typography>
        </div>
      );
    };
    
    export default Customers;
    